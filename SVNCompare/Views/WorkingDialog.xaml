<Window x:Class="SVNCompare.WorkingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WorkingDialog" Height="154" Width="456" ResizeMode="NoResize">
    <Grid>
        <ProgressBar x:Name="prgbProgress" Height="29" Margin="0,10,0,0" VerticalAlignment="Top" Width="430" Value="57"/>
        <Label x:Name="lblCurrentFile" Content="lblCurrentFile" Margin="0,44,0,0" VerticalAlignment="Top" Width="430"/>
        <Button x:Name="btnCancel" Content="Cancel" HorizontalAlignment="Center" Margin="10,70,0,0" VerticalAlignment="Top" Width="107" Height="35" Click="btnCancel_Click"/>

    </Grid>
</Window>
